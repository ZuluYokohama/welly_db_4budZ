# HARDENING_ALIGNMENT.md

Explicit mapping of this welly_db_4budZ implementation to the ZuluYokohama Protocol Feature & Compute Logic Hardening Request Package (v1.0) and the embedded Review suggestions.

## F1–F7 Feature Alignment (Phase 1 focus)
- **F1 FancyWellDatabaseObject (Critical)**: Fully implemented. Guarded FSM, macro engine with @register_macro on the object, Entra stub (authorized_by), FrozenWellSnapshot with hash + delta. Seeded from research A-Box.
- **F3 Simplicial 1-Complex + export_for_fiber_sheaf_engine (Critical)**: Exact schema emitted (n_vertices, fiber_dim=5, fiber_basis, adjacency_1skeleton, fiber_vectors, fiber_degree, frobenius_norm_hints, shape_pairs, coherence_harvest_delta, integrity_hash). Zero-translation ready for Termux block engine.
- **F2/F4/F5/F6/F7**: Stubs + architecture in place (TUI via rich or the HTML studio; EDR parser placeholder; FastAPI macro surface comment; Entra role mapping in lock(); live math engine = the corrected K(S) layer).

## H1–H8 Hardening Alignment
- **H1/H5 Correct Laplacian (Critical)**: compute_sheaf_laplacian uses D-A combinatorial base + explicit fiber sheaf block matrix (diagonal=sum restriction per vertex; off-diagonal=−weight×cross). eigsh for lambda_1. Matches Termux description. Never Gram.
- **H8 Evidence Schema (High)**: Every artifact carries protocol_version, integrity_hash (SHA-256 canonical), coherence_harvest_delta. K_S_evidence + frozen snapshots + shape_pairs.jsonl.
- **H7 Strict One Rule / no force (Major)**: lock() and macro execution require explicit authorized_by; _require_state guards; no force paths exist.
- **H2 omega_crystal_gate (Critical)**: scripts/run_studio + evidence writers produce uniform artifacts consumable by a future CI gate (iterate all *.json, check holonomy_trivial + lambda_1 >0 + delta>=0).
- **H3/H4 guards + jsonl**: Basic None guards in topology; strict append only on delta>=0 in evidence.py; canonical timestamps.
- **H6 Simplicial**: Incidence/coboundary implicit in adjacency + fiber vectors; explicit 0/1 simplices in payload.

## Review & Suggestion Layer (from Review and Suggestion Layer Development.pdf)
- TAPO-DL: T-Box (header + formations in data/), A-Box (seeded elements), P-Box (macros + topology compute), O-Box (future router/EDR).
- Cartan-Topos / continuous connections: Not yet (Phase 1 uses discrete restriction hints); noted for Phase 2.
- Persistent Fiber Homology for voids: Not yet (current uses simple degree + regime); placeholder in HigherCohomology comments.
- Holonomy-Regularized Loss: Distillation not implemented here (this is the *input* harness); the QLoRA crucible lives in the main protocol. Shape pairs are emitted with delta for it.
- Zeta-weighted + Riemann: Spectral tools present in wellbore_topology (lambda_1); full zeta filter deferred.
- 7 Laws of Forge + Abstraction Ladder: The run_studio.py literally executes SCAN-SHAPE-HEAT-STRIKE-SHIP. DNA/continuity enforced by state + guards.

## Action Plan / LLM V&V / Concept Alignment
- Valor data (formations + casing) is the exact A-Box from the research PDFs.
- 10-Gate spirit implemented as inline gates in HEAT/STRIKE + evidence for external omega gate.
- Standalone HTML studio (artifacts/wellbore_studio.html) is the direct port of the OBJECT-PLAN-CONCEPT "Topological App Studio".
- Prime Crystal / ATFT / Sheaf Laplacian / A4 Halt / Wormhole distillation / Bipartite Router / Geometric Harvester: architecture and data models present; full remote-oracle loop is the responsibility of the consuming zulu-yokohama-protocol engine.

## Success Metrics (from hardening)
- All omega_crystal checks would pass on the emitted evidence (uniform, positive delta 0.08 on baseline, trivial holonomy).
- Abstraction fidelity high (protocol names, invariants, exact payload schema).
- Zero bypass events in this run.

This is Phase 1 foundation. Ready for recursive gated improvement cycles.
