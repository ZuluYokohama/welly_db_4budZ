# PROTOCOLS.md — welly_db_4budZ

This module implements core invariants and objects from the **ZuluYokohama Protocol**.

## Core Thesis (this harness)
The mathematics are the operating system. Zero bypass. Full structure.

welly_db supplies the **FancyWellDatabaseObject** (guarded upstream input harness) that feeds clean, coherence-gated WellGraph / simplicial payloads into the Prime Crystal Engine, fiber sheaf block Laplacian K(S), Δλ₁ gating, Bipartite Router, and closed-loop distillation — without ever compromising the protocol invariants.

## Implemented / Aligned Components (Phase 1)
- **F1** FancyWellDatabaseObject: state machine (OPEN/TOGGLED_*/LOCKED), @register_macro, frozen snapshots with SHA-256 + coherence_harvest_delta, Entra role stub.
- **F3** Simplicial 1-Complex + export_for_fiber_sheaf_engine(): exact block_laplacian_ready schema.
- **H1/H5** Corrected Laplacian: compute_sheaf_laplacian uses proper block construction (not Gram matrix).
- **H8** Evidence Schema: integrity_hash, protocol_version, coherence_harvest_delta on artifacts.
- **H7** Strict "One Rule" / no force bypass on lock/export.
- TAPO boxes (T-Box = header + formations; A-Box = seeded Valor elements; P-Box = macros + topology compute; O-Box = future EDR / remote router).
- 5-step Forge pipeline (SCAN-SHAPE-HEAT-STRIKE-SHIP) in scripts/run_studio.py.
- Basic A4-style continuity guard (holonomy trivial + Δλ₁ >= 0 before ship).

## Key Invariants Enforced Here
- Depth strictly increases (restriction map).
- Positive physical values only (tmd > 0, od > 0).
- Regime colors map to hazard scores (Stable low, Critical high).
- All exports carry cryptographic integrity + harvest delta.
- Locked snapshots are immutable.

## References (the governing documents)
See root README for the full list of PDFs this was built under.

## Next (Phase 2+)
- Full Entra MSAL (F6)
- Desktop TUI (F2) + EDR ingestion (F4)
- Live Termux math engine parity (F7)
- 10-Gate omega_crystal_gate CI enforcement (H2)
- Persistent Fiber Homology + Cartan-Topos restriction maps (review suggestions)
- Holonomy-Regularized loss in distillation (review)

"Mathematics are the Operating System. Zero Bypass. Full Structure."
